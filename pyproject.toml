[tool.poetry]
name = "h3-multi-resolution-index"
version = "0.1.0"
description = "H3 multi-resolution spatial index with DuckDB backend"
authors = ["Sebastian Gmur"]
packages = [{include = "h3_multi_resolution_engine"}]

[tool.poetry.dependencies]
python = "^3.11"
h3 = "^4.0.0"
duckdb = "^1.2.0"
shapely = "^2.0.0"
geopandas = "^1.0.0"
pyproj = "^3.6.0"

[tool.poetry.group.dev.dependencies]
pytest = "^8.0.0"

[build-system]
requires = ["poetry-core"]
build-backend = "poetry.core.masonry.api"
